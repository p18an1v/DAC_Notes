#include<iostream>
#include<thread>
using namespace std;


void myfun1()
{
	cout << "inside myfun1 function of thread" << endl;
}
int main()
{
	cout << "in main function before" << endl;
	thread t1(myfun1);
	// thread t1{ myfun1 }; // this syntax is also valid
	//t1.join();  // this is must
	cout << "in main function after" << endl;
	for (int i = 0; i < 10; i++)
	{
		cout << "Hello\t" << i << endl;
	}
}