#include<iostream>
#include<thread>
using namespace std;
// multithreading using lambda 

int main()
{
	int val = 400;
	thread t1([]() {cout << "id of a user defined thread is\t"<<this_thread::get_id()<<endl;});
	t1.join();  // this is must
	cout << "id of a main thread is\t"<<this_thread::get_id()<<endl;
}