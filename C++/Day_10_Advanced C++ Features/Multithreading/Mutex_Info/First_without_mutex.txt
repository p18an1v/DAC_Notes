#include<iostream>
#include<thread>
#include<windows.h>
using namespace std;


void myfun()
{
	for (int i = 0; i < 10; i++)
	{
		cout << "Hello\t" << i << "\t by\t" << this_thread::get_id() << endl;
		Sleep(200);
	}
}
int main()
{
	thread t1(myfun);
	thread t2(myfun);
	t1.join();
	t2.join();
	cout << "inside main thread" << endl;
}